var numbers = [1, 2, 3, 4, 5, 6, 7, 8];
  for (var i = 0; i < numbers.length; i++) {
    if (numbers[i] < 60) {
      console.log(numbers[i]);
    } else if (numbers[i] > 30) {
      console.log(numbers[i]);
    }
  }
